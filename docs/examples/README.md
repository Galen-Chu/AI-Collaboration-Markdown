# Atlas

Atlas is a cron scheduler with run history.

## Quick Start

```bash
npm install
npm run dev
```

## What it does

Runs scheduled tasks (HTTP or shell), records run results, exposes them via API.

## Docs

See [INDEX.md](./INDEX.md) for the full doc set.
